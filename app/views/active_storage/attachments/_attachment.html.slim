li.attachment
  = link_to attachment.blob.filename, rails_blob_path(attachment)
  - if (delete_link ||= false)
    =< link_to '[×]', attachment, class: 'delete_attachment', method: :delete, remote: true, data: { confirm: 'Are you sure?' }
